import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.hinryd.gol',
  appName: "Conway's game of life",
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
